version: '3.8'

services:
  server:
    build: ./server
    ports:
      - "3001:3001"
    env_file:
      - ./server/.env
    restart: always

  client:
    build: ./client
    ports:
      - "3000:80"
    env_file:
      - ./client/.env
    depends_on:
      - server
    restart: always
